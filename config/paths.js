'use strict';

const path = require('path');

const PATHS = {
  src: path.resolve(__dirname, '../src'),
  build: path.resolve(__dirname, '../build'),
  wbuild: path.resolve(__dirname, '../build/w'),
};

module.exports = PATHS;
